# Versiones con las que se ejecutó y probó el notebook de principio a fin
# (25 de septiembre de 2026). Si usas Google Colab, numpy/pandas/scipy/
# scikit-learn/tensorflow ya vienen preinstalados; yfinance no, por eso
# es indispensable fijarlo aquí igual que el resto del stack.

numpy==2.1.3
pandas==2.2.3
scipy==1.16.3
scikit-learn==1.6.1
tensorflow==2.20.0
yfinance==0.2.66
matplotlib==3.9.2
